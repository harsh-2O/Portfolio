import type { BlogPost } from '../types';

export const blogPosts: BlogPost[] = [
  {
    id: 1,
    title: 'Production RAG over Market Data',
    subtitle: 'AI · Retrieval · Financial Time Series',
    date: 'Jun 2026',
    readTime: '14 min',
    tags: ['RAG', 'LLMs', 'BigQuery', 'Vector Search', 'Quant'],
    accent: '#0071e3',
    excerpt:
      'Why naive RAG breaks on tick-level financial data — and the full architecture I used to ship 30+ production APIs over stock market datasets in 22 days at SEARCE.',
    highlights: [
      'Event-aware chunking with instrument + session metadata',
      'Hybrid retrieval: vectors + SQL for precise ticker lookups',
      'Cross-encoder reranker cut hallucinations more than new embeddings',
      'Golden eval set of 150+ quant-researcher queries',
    ],
    content: [
      'Most RAG tutorials assume static documents: PDFs, wikis, support tickets. Market data is a different species entirely. Schemas shift when exchanges roll out new feed versions. Symbols rename overnight. Corporate actions — splits, dividends, spin-offs — rewrite historical prices retroactively. And unlike a customer support bot, your latency budget isn\'t "a few seconds feels fine." Quant researchers measure retrieval quality in whether they trust a number enough to put capital behind it.',
      'At SEARCE I built a production-grade RAG platform over large-scale stock market data, exposing 30+ backend APIs in 22 days. The system enabled natural-language queries over structured and semi-structured financial datasets — think "show me unusual volume spikes in semiconductor stocks during the last Fed announcement week" without writing SQL. This post covers what actually worked, what failed, and the patterns I\'d reuse on any financial RAG system.',
      '**The core insight: retrieval is a data engineering problem first.** Before you touch LangChain or prompt templates, you need clean ingestion pipelines, stable schemas, metadata-rich chunks, and a way to measure retrieval quality independently of generation. LLMs are the presentation layer — not the foundation.',
      '**Problem 1 — Naive chunking destroys temporal context.** Fixed 512-token windows split earnings tables across chunks, orphan ticker symbols from their price series, and lose the relationship between corporate action events and adjusted prices. Our fix: event-aware chunking. Group data by instrument and trading session. Attach metadata to every vector: exchange, asset class, adjustment type, data freshness timestamp, and source pipeline version. A chunk should answer "what, when, where, and how trustworthy" without needing neighboring chunks.',
      '**Problem 2 — Pure semantic search fails on precise queries.** Embeddings excel at fuzzy conceptual matching — "tech sector momentum" — but fail on exact lookups: "AAPL split-adjusted close on 2024-01-15" or "total volume for NVDA on options expiry Friday." We implemented hybrid retrieval: dense vector search for exploratory queries, plus structured metadata filters (date range, ticker, exchange, metric type) applied before or after retrieval. For numeric lookups, we routed to SQL/BigQuery directly via a query classifier — the LLM decided whether to retrieve documents or execute structured queries.',
      '**Problem 3 — Stale embeddings silently degrade answers.** Market data pipelines refresh on schedules — end-of-day batches, intraday snapshots, corporate action replays. Cached embeddings without TTLs will confidently return yesterday\'s truth. We tied embedding invalidation to pipeline completion events. Symbol-level indexes were purged on corporate action ingestion. Each chunk carried a `valid_until` metadata field the retriever checked before ranking.',
      '**Architecture overview.** Ingestion layer (BigQuery + scheduled ETL) → chunking service with metadata enrichment → embedding pipeline (batch + incremental) → vector store with hybrid index → retrieval orchestrator (classifier + reranker) → LLM generation with citation requirements → API gateway (30+ endpoints). Each layer had independent monitoring. Retrieval and generation were evaluated separately.',
      '**The reranker mattered more than the embedding model.** Initial retrieval pulled top-20 candidates via hybrid search. A cross-encoder reranker trimmed to top-5 before context injection. This single step cut hallucination rates more than switching embedding models. Financial queries are high-stakes — precision at the retrieval stage saves you from eloquent wrong answers.',
      '**Prompt design for financial context.** We required the LLM to cite chunk IDs and timestamps in every factual claim. Responses included a confidence qualifier when data freshness exceeded thresholds. The system prompt explicitly forbade extrapolation beyond retrieved context — "I don\'t have data for that period" was a valid and encouraged response. In finance, a honest gap beats a fabricated number.',
      '**Evaluation framework.** We built a golden set of 150+ queries from quant researchers and data analysts — categorized into lookup, comparison, trend, and exploratory types. Metrics: retrieval precision@5, recall@10, answer faithfulness (human-rated), and citation accuracy. We ran evals on every embedding model upgrade and pipeline schema change. Production RAG without evals is hope-driven development.',
      '**Latency and cost.** Batch embedding for historical data, incremental for daily updates. API p95 under 2 seconds for retrieval + generation with streaming responses. Cached frequent queries at the API layer with freshness headers. LLM costs were manageable once retrieval was precise — smaller, higher-quality context windows beat dumping entire tables into prompts.',
      '**Lessons learned.** (1) Invest in metadata — it\'s the bridge between fuzzy search and financial precision. (2) Separate structured and unstructured retrieval paths. (3) Measure retrieval independently. (4) Design for staleness from day one. (5) Citations aren\'t optional in financial AI. The teams that ship reliable market data RAG treat it as a data product with an LLM interface — not an LLM product with data bolted on.',
    ],
  },
  {
    id: 2,
    title: 'MCP & Agentic AI for Quant Developers',
    subtitle: 'AI Dev Tools · Automation · Cursor',
    date: 'May 2026',
    readTime: '12 min',
    tags: ['MCP', 'Cursor', 'Agents', 'Claude', 'Automation'],
    accent: '#5856d6',
    excerpt:
      'A deep dive into how Model Context Protocol and Cursor agent workflows cut my AWS Lambda refactoring cycle 4× — and how to build quant-aware AI tooling that doesn\'t hallucinate your infrastructure.',
    highlights: [
      'MCP servers for GitHub, APIs, and exchange spec files',
      'Cursor rules + MCP: 4 Lambdas in 4 days (was 4 days each)',
      'Version-controlled agent skills in markdown',
      'Read-only prod access by default — writes need approval',
    ],
    content: [
      'The bottleneck in quant engineering isn\'t writing code — it\'s the constant context switching. You\'re in a Go feed handler, then an exchange spec PDF, then a Grafana dashboard, then a symbol mapping CSV, then back to code. Autocomplete helps you type faster. It doesn\'t help you reason across systems. That\'s the gap agentic AI fills — if you give agents structured access to real tools instead of hoping they guess from training data.',
      'Model Context Protocol (MCP) is the open standard that makes this possible. MCP servers expose tools — query a database, read a file, call an API, validate a config — in a format any compatible agent can invoke. Cursor, Claude Desktop, and increasingly other clients speak MCP natively. For quant devs, this means your AI assistant can check live exchange configs instead of inventing field names.',
      '**My setup in practice.** I run MCP servers for: GitHub (PR diffs, issue context, CI status), internal REST APIs (symbol mappings, feed health checks), and filesystem access to exchange spec directories. Each server is scoped with least-privilege credentials. The agent composes these tools during a task — "read the CME spec update, diff against our current config, draft the migration PR" — without me copy-pasting context between tabs.',
      '**Cursor rules + MCP = engineering standards with live data.** My `.cursorrules` file enforces stack conventions: Go error wrapping patterns, no hardcoded secrets, Emotion styled-components without bare component selectors, test coverage expectations for exchange onboarding code. MCP provides the data layer. Together they transformed a refactoring project: AWS Lambda migrations across Go, Python, and Java went from 4 days per Lambda to 4 Lambdas in 4 days. The agent wasn\'t writing perfect code autonomously — it was doing the tedious 80% (boilerplate conversion, import updates, test scaffolding) while I reviewed architectural decisions.',
      '**Agent skills as version-controlled prompts.** I package reusable workflows as markdown skill files: "Exchange Onboarding Checklist," "Incident Triage for Feed Gaps," "PR Security Review." Each skill defines inputs, tool calls, output format, and stop conditions. Skills live in the repo alongside code — reviewed in PRs, versioned, rollback-able. This is how you scale agent workflows across a team without everyone inventing their own prompts in chat history.',
      '**The MCP + RAG combination.** For documentation-heavy tasks, I combine MCP filesystem access with a local RAG index over exchange specs and internal runbooks. The agent retrieves relevant spec sections, then uses MCP to validate current configs against retrieved requirements. Retrieval grounds the agent; MCP verifies against reality. Neither alone is sufficient for production engineering.',
      '**Safety boundaries for trading infrastructure.** Agents get read-only access to production-adjacent systems by default. Write operations — config changes, deployment triggers, database migrations — require explicit human approval per action. Tool calls are logged with inputs and outputs. We treat agent actions like CI pipeline steps: auditable, reversible, and never silently auto-approved for high-risk operations. In trading infra, a confident wrong automation is worse than a slow correct human.',
      '**Anthropic Claude + LinkedIn Learning MCP courses.** I completed MCP hands-on training (Anthropic\'s Claude 3.5 and MCP protocol courses on LinkedIn Learning) and applied it immediately: built MCP servers for stock data query APIs and integrated them into Cursor for conversational analytics development. The courses are practical — you build real servers, not slide decks.',
      '**Common failure modes.** (1) **Over-trusted agents** — shipping agent output without review. (2) **Under-scoped tools** — one mega MCP server with admin access to everything. (3) **No evals** — assuming the agent "seems right." (4) **Prompt drift** — skills not versioned, everyone uses different instructions. (5) **Ignoring latency** — agents making 15 sequential tool calls when one batched query would do. Treat agent workflows as distributed systems with failure modes.',
      '**Quant-specific agent use cases that work today.** Config diffing across exchange environments. Generating monitoring alert rules from incident postmortems. Scaffolding Go test fixtures from production log samples (sanitized). Translating exchange spec tables into internal schema definitions. Drafting runbook updates after onboarding a new feed. These are high-context, low-risk-writing tasks where agents excel.',
      '**What doesn\'t work yet.** Autonomous exchange onboarding end-to-end. Production trading logic changes without human review. Anything requiring sub-millisecond performance decisions. Agents are engineering accelerators, not engineering replacements — especially in quant.',
      'The future of quant dev tooling isn\'t smarter autocomplete. It\'s orchestrated agents with auditable tool access, version-controlled skills, and retrieval grounded in your actual systems. MCP is the plumbing layer making that real today — and the teams adopting it now are compressing delivery timelines measurably.',
    ],
  },
  {
    id: 3,
    title: 'Low-Latency Exchange Onboarding',
    subtitle: 'Quant · Market Data · Go · C++',
    date: 'Apr 2026',
    readTime: '15 min',
    tags: ['Go', 'Market Data', 'Exchanges', 'C++', 'Low-latency'],
    accent: '#34c759',
    excerpt:
      'A field guide to onboarding global exchanges at Graviton — covering feed handlers, symbol resolution, tick validation, and why data correctness matters more than integration speed.',
    highlights: [
      '5-phase pipeline: spec → feed → symbols → validation → distribution',
      'Quarantine queue for bad ticks — never silently forward',
      'Go for control plane orchestration, C++ for data plane',
      'Parallel feed diff before cutover to production',
    ],
    content: [
      'At Graviton Research Capital I own market data infrastructure and exchange integrations across 12 global exchanges — primary owner for 9. Exchange onboarding is the highest-risk, highest-complexity operation in a quantitative trading stack. One incorrect symbol mapping, one missed corporate action, one silent sequence gap — and every downstream strategy inherits the error. PnL doesn\'t forgive bad data.',
      'This isn\'t an API integration tutorial. Exchange onboarding in production quant is a **data correctness problem at scale** with tight latency budgets, heterogeneous feed formats, and zero tolerance for silent failures. Here\'s how we approach it.',
      '**Phase 0 — Specification ingestion.** Before writing code, ingest the exchange\'s complete spec: feed format (binary FIX, ITCH, proprietary), symbol reference files, trading calendar, corporate action policies, failover procedures. Version-control spec documents. Diff when exchanges publish updates. I\'ve seen more production incidents from spec drift than from code bugs.',
      '**Phase 1 — Feed handler.** Connect to the exchange feed (live or replay). Parse messages into a normalized internal tick format: timestamp (exchange-time and receive-time), instrument ID, price, quantity, trade vs quote indicator, sequence number. Log raw messages during onboarding for forensic replay. Never trust a parser until you\'ve compared output against the exchange\'s own sample data.',
      '**Phase 2 — Symbol resolution.** Exchanges use incompatible identifiers: RIC codes, ISIN, internal numeric IDs, bloomberg tickers. Maintain a versioned mapping table: exchange_symbol → internal_instrument_id. Automate diff detection when reference files update. Flag ambiguous mappings for human review — auto-resolving "similar looking" symbols is how you trade the wrong contract.',
      '**Phase 3 — Tick validation.** This is where onboarding quality is won or lost. Checks we run continuously: sequence number monotonicity (gap detection), price bounds (circuit breaker sanity), timestamp ordering, crossed book detection, trade price vs NBBO consistency, volume spike anomalies. Failed ticks go to a quarantine queue — never silently dropped, never forwarded to production consumers. Researchers need to know data is clean or know that it isn\'t.',
      '**Phase 4 — Corporate actions.** Splits, dividends, mergers, delistings — exchanges apply these differently. Some adjust historical prices, some don\'t. Some send explicit messages, some update reference files silently. Build explicit corporate action ingestion and replay historical ticks through adjustment pipelines. Validate against exchange-provided adjusted series before promoting.',
      '**Phase 5 — Distribution & consumer readiness.** Validated ticks flow to internal distribution (message bus, shared memory, file replay). C++ alpha frameworks and PnL systems subscribe. During onboarding, run parallel feeds: old production source vs new exchange feed. Diff at the tick level. Discrepancy thresholds gate the cutover decision.',
      '**Automation with Go.** I engineered automation pipelines for exchange configurations, symbol mappings, and monitoring workflows in Go. Go\'s concurrency model fits orchestration: parallel health checks, config generation, deployment coordination. It\'s not in the hot tick path — it\'s the control plane. C++ handles the data plane where nanoseconds matter.',
      '**Monitoring from day one.** Per-exchange dashboards: message rate, gap count, quarantine queue depth, mapping unresolved count, latency percentiles (exchange-time to internal-distribution). Alerts page on-call before researchers notice. Onboarding isn\'t complete when the feed connects — it\'s complete when monitoring proves stability over multiple trading sessions including high-volatility days.',
      '**Cross-exchange consistency.** With 12 exchanges, consistency in internal representation matters enormously. Same timestamp semantics, same instrument ID scheme, same corporate action handling — even when underlying feeds differ radically. Standardize at the normalization layer. Let exchange-specific quirks live in parsers, not in downstream consumer logic.',
      '**Operational rigor checklist.** Spec version pinned. Parser tested against exchange samples. Symbol mapping reviewed by second engineer. Tick validation rules documented. Corporate action handling validated. Parallel feed diff clean for N sessions. Monitoring and alerts live. Rollback procedure documented and tested. Onboarding is 20% code, 80% process.',
      '**What I\'ve learned.** Speed of onboarding matters less than correctness of onboarding. A delayed exchange launch beats a corrupted one. Quant researchers will forgive a late feed; they won\'t forgive a wrong one. Treat market data as a first-class product with SLAs, owners, and incident response — not a side effect of trading infrastructure.',
    ],
  },
  {
    id: 4,
    title: 'ML Research → Production in Quant',
    subtitle: 'AI/ML · ONNX · Model Quantization · C++',
    date: 'Mar 2026',
    readTime: '13 min',
    tags: ['PyTorch', 'ONNX', 'Quantization', 'C++', 'Alpha'],
    accent: '#ff9500',
    excerpt:
      'The complete path from Jupyter notebooks to C++ alpha frameworks — export pipelines, quantization trade-offs, feature parity traps, and production monitoring for ML in trading systems.',
    highlights: [
      'Feature parity tests: research vs C++ must match bit-for-bit',
      'ONNX export → C++ harness → latency profiling before integration',
      'Shadow mode + canary rollout — never binary flag flip',
      'Monitor prediction drift, not just inference speed',
    ],
    content: [
      'Quant ML has a deployment cliff that batch ML teams never face. Researchers prototype in Python — PyTorch, pandas, Jupyter — exploring signals on historical tick data. Production alpha runs in C++ with microsecond budgets, strict memory layouts, and zero Python in the hot path. The gap between notebook and production isn\'t a `torch.onnx.export()` call. It\'s an engineering discipline.',
      'At Graviton I work on internal quantitative tooling including PnL systems and C++ alpha frameworks. ML models that generate or enhance signals must survive this transition — or they stay in research forever. This post is the pipeline I\'ve seen work, and the traps that kill projects.',
      '**Stage 1 — Research with production constraints in mind.** Before training, define: inference latency budget (microseconds per tick), feature availability in production (can this data arrive in time?), retraining cadence, and fallback behavior if the model fails. Research that ignores these constraints produces beautiful backtests that can never ship.',
      '**Stage 2 — Feature pipeline parity.** The silent killer. Research computes features in pandas with forward-fill, rolling windows, and NaN handling in a specific order. Production computes the same features in C++ on streaming ticks. A one-line difference — normalization before clip vs after, timezone handling, dividend adjustment order — flips strategy PnL. Version feature definitions alongside model weights. Run parity tests: same input tensor → research output must match production output bit-for-bit (or within defined tolerance).',
      '**Stage 3 — Export to ONNX.** Train in PyTorch. Validate offline on held-out data with realistic transaction costs. Export via ONNX with explicit input/output specs. Validate ONNX model against PyTorch on thousands of random inputs. Check opset compatibility with your C++ inference runtime. Document input tensor shape, dtype, and preprocessing requirements in a model card.',
      '**Stage 4 — C++ inference harness.** Build a benchmark harness that loads ONNX via ONNX Runtime (or equivalent), feeds production-representative input tensors, and measures p50/p99 latency and memory allocation patterns. Profile before integrating into alpha framework. A model that runs in 50μs in isolation but allocates heap per tick will die in production.',
      '**Stage 5 — Quantization.** INT8 and FP16 quantization cut latency and memory — but shift signal distributions. Always A/B quantized vs float32 on historical tick replay through the full alpha pipeline. Measure PnL impact, not just inference speed. Some models tolerate quantization; others destroy edge. Quantize per-layer if global quantization degrades critical features. Document quantization scheme in the model card.',
      '**Stage 6 — Gradual rollout.** Never flip a binary flag from old signal to new model. Shadow mode: run new model alongside production, log outputs, compare distributions. Canary: route small capital allocation to new signal. Full promotion only after statistical confidence that live behavior matches backtest expectations. Feature flags for instant rollback.',
      '**Production monitoring.** Track: prediction distribution drift (KL divergence vs training set), input feature drift, inference latency percentiles, error rate (NaN outputs, exceptions), and downstream PnL attribution per signal source. ML models decay in live markets faster than batch systems — regime changes eat static models. Define retraining triggers: drift threshold exceeded, scheduled calendar, or manual promotion of research candidate.',
      '**Audio ML crossover — APE project.** My Audio Personality Emulator (APE) project trained regression models on MFCC audio features — a different domain, but the same lessons apply: feature extraction parity between training and inference, export discipline, and evaluating on held-out real-world samples. Domain changes; engineering doesn\'t.',
      '**Common failure modes.** (1) **Notebook-only validation** — no streaming simulation. (2) **Feature leakage in research** — future data sneaks into features that won\'t exist in production. (3) **Ignoring inference cost** — model works but too slow for tick frequency. (4) **No rollback** — production incident becomes a fire drill. (5) **Research/production team silos** — nobody owns the gap.',
      '**The organizational insight.** Compress research-to-production latency by making it someone\'s job — a quant dev who speaks both PyTorch and C++, who owns the export pipeline, who sits in research meetings asking "can we ship this?" from day one. The technology exists. The bottleneck is usually organizational.',
      'The goal isn\'t deploying ML for its own sake. It\'s getting validated signals to the trading floor while they still have edge. Every week a model sits in a notebook is a week of alpha decay. Build the pipeline, measure the pipeline, trust the pipeline — then iterate on models, not on deployment heroics.',
    ],
  },
];
